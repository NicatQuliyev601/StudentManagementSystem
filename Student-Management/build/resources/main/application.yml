spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Student-Management
    username: postgres
    password: 0308

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 8099